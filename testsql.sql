

create table student2 (id int, name varchar(20), age int, sex int);

select * from student;

insert into student (id, name, age, sex) values (5555, '伊子米', 20, true);

delete from student where student.age = 18;